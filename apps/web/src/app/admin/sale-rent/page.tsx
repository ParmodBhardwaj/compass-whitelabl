/**
 * /admin/sale-rent — Sale/Rent landing.
 * Re-exports the Categories admin since "Sale/Rent" parent landing
 * has the same data shape (categories list is the primary view).
 */
export { default } from '@/app/admin/sale-rent/categories/page';
