/**
 * /admin/sopOperation/feedback — SOP (Plant Operation) Feedback inbox.
 * Identical to /admin/feedback (same `sop_feedback` table). Re-export.
 */
export { default } from '@/app/admin/feedback/page';
